<!DOCTYPE html>


<head>
    {% block head %}
        <title>CSS Navigation</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
        <link rel="stylesheet" href="{{ asset('bundles/mercuryseriesflashy/css/flashy.css') }}">
        <script src="{{ asset('bundles/mercuryseriesflashy/js/flashy.js') }}"></script>
        <link href="//fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        <link href='//fonts.googleapis.com/css?family=Source+Sans+Pro:300,400,600,700' rel='stylesheet'>
        <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css')}}">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    {% endblock %}
</head>

<body>
<style>
    .banner-image{
        background-image: url("/uploads/images/img.jpg");
        background-size: cover;

    }

    .dropbtn {
        background-color: inherit;
        color: #2F323A;
        padding: 12.5px;
        font-size: 16px;
        border: none;
        cursor: pointer;
    }

    .dropdown {
        position: relative;
        display: inline-block;
    }

    .dropdown-content {
        display: none;
        position: absolute;
        background-color: #6c757d;
        min-width: 160px;
        box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
        z-index: 1;
    }

    .dropdown-content a {
        color: black;
        padding: 12px 16px;
        text-decoration: none;
        display: block;
    }

    .dropdown-content a:hover {background-color: #f1f1f1}

    .dropdown:hover .dropdown-content {
        display: block;
    }

    .dropdown:hover .dropbtn {
        background-color: inherit;
    }
    .textW{
        color: #fff;
    }
    .container{
        display: flex;
        flex-wrap: wrap;
        justify-content: space-between;
    }
</style>
{% block nabvar %}

<nav class="navbar fixed-top navbar-expand-lg navbar-dark p-md-3">
    <div class="container">
        <a href="#" class="navbar-brand" > </a>
        <button type="button" class="navbar-toggler" data-bs-target ="#navbarNav" data-bs-toggle="collapse" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle Navbar"> <span class="navbar-toggler-icon">

            </span></button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <div class="mx-auto"></div>

            <ul class="navbar-nav">
                <li class="nav-item">
                    <a href="{{ path('afficherPrCl') }}" class="nav-link text-white" > Home</a>

                </li>
                <li class="nav-item">
                    <a href="{{ path('afficherClLogo',{'id':app.user.idcl}) }}" class="nav-link text-white"> Logo</a>

                </li>
                <li class="nav-item">
                    <a href="{{ path('afficherClCh',{'id':app.user.idcl}) }}" class="nav-link text-white"> Chart</a>

                </li>

                <li class="nav-item dropdown">
                    <button class="dropbtn">Profil</button>
                        <div class="dropdown-content">

                    <a href="{{ path('afficherProfilCl',{'id':app.user.idcl}) }}" class="nav-link text-white dropbtn"> afficher mon profil</a>
                       <a href="{{ path('afficherClfacture',{'id':app.user.idcl}) }}" class="nav-link text-white dropbtn"> Factures</a>
                       <a href="{{ path('afficherClContrat',{'id':app.user.idcl}) }}" class="nav-link text-white dropbtn"> Contrats</a>
                       <a href="{{ path('afficherrecCl',{'id':app.user.idcl}) }}" class="nav-link text-white dropbtn"> Reclamations</a>

                   </div>
                </li>
                {% if app.user.auth==1 %}
                    <li class="nav-item">
                        <a href="{{ path('admin')}}" class="nav-link text-white"> Espace admin</a>

                    </li>
                {% endif %}
                <li class="nav-item">
                    <a href="{{ path('app_logout') }}" class="nav-link text-white">Logout</a>


                </li>
            </ul>
        </div>
    </div>
</nav>
{% endblock %}
{% block banner %}
<div class="banner-image w-100 vh-100 d-flex justify-content-center align-items-center" >
    <div class="content text-center">
        <h1 class="text-white">Welcome</h1>
    </div>
</div>
{% endblock %}
{% block content %}
<div class="container">


</div>
{% endblock %}
{% block container %}
{% endblock %}

{% block script %}

 <script src="{{ asset('js/bootstrap.bundle.min.js')}}">
        var nav = document.querySelector('nav');

        window.addEventListener('scroll', function () {
            if (window.pageYOffset > 100) {
                nav.classList.add('bg-dark', 'shadow');
            } else {
                nav.classList.remove('bg-dark', 'shadow');
            }
        });


    </script>
{% endblock %}

</body>
<html>