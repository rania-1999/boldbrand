
{% extends '/back.html.twig' %}
{% block content %}

    <div>
    {{ form_start(form,{'attr':{'novalidate':'novalidate'}}) }}

    <h1 >
        Modifier une Reclamation
    </h1>

    <!--begin::Form-->
    <!--ajoute1r un evenement-->

    <div>

        <div> {{ form_label(form.obj,'Objet: ') }}</div>
        {{ form_errors(form.obj) }}
        {{ form_widget(form.obj) }}
    </div>
    <br>
    <div>
        <div>    {{ form_label(form.msg,'message: ') }}</div>
        {{ form_errors(form.msg) }}
        {{ form_widget(form.msg) }}
    </div>
    {{ form_end(form) }}
    </div>{% endblock %}